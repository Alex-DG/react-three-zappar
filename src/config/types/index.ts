export type ButtonProps = { primary?: boolean }

export type ExperienceOptions = {
  targetElement: HTMLDivElement
}

export type ExperienceConfig = {
  pixelRatio: number
  width: number
  height: number
}
